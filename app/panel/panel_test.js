'use strict';

describe('jukebuzz.panel module', function() {

  beforeEach(module('jukebuzz.panel'));

  describe('panel controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var panelCtrl = $controller('PanelCtrl');
      expect(panelCtrl).toBeDefined();
    }));

  });
});