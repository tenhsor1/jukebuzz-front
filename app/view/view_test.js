'use strict';

describe('jukebuzz.view module', function() {

  beforeEach(module('jukebuzz.view2'));

  describe('jukebuzz controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var viewCtrl = $controller('ViewCtrl');
      expect(viewCtrl).toBeDefined();
    }));

  });
});